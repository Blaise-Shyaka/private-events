<nav>
  <% if session[:name] %>
    <%= link_to  "Log Out", session_path(session[:name]), method: :delete %><br>
    <br>
    <p>You are Log In as: <br> <strong> <%= session[:name] %></strong></p>
    
    <%= link_to "Create new event", new_event_path %><br><br>
  <% else %>
    <p>You need to Log In or Sign Up</p>
    <%= link_to  "Log In", new_session_path%>
    <%= link_to  "Sign Up", new_user_path%> <br>
  <% end %>
  <%= link_to  "See all your Events", events_path%> <br>
</nav>



<% if @user_created_events %>
  <div class="my-events-cont">
    <h2>Created Events</h2>
    <ul class="my-events">
      <% @user_created_events.each do |me| %>
        <li>
          <%= link_to  me.name, event_path(me.id) %>
        </li>
        <li>
        <%= @user_created_events.title %>
        </li>
        <li>
        <%= @user_created_events.description %>
        </li>
        <li>
        <%= @user_created_events.venue %>
        </li>
        <li>
        <%= @user_created_events.date %>
        </li>
      <% end %>
    </ul>
  </div>
<% end %>

<% if @past_events %>
  <div class="my-events-cont">
    <h2>Created Events</h2>
    <ul class="my-events">
      <% @past_events.each do |me| %>
        <li>
          <%= link_to  me.name, event_path(me.id) %>
        </li>
        <li>
        <%= @past_events.title %>
        </li>
        <li>
        <%= @past_events.description %>
        </li>
        <li>
        <%= @past_events.venue %>
        </li>
        <li>
        <%= @past_events.date %>
        </li>
      <% end %>
    </ul>
  </div>
<% end %>

<% if @upcoming_events %>
  <div class="my-events-cont">
    <h2>Created Events</h2>
    <ul class="my-events">
      <% @upcoming_events.each do |me| %>
        <li>
          <%= link_to  me.name, event_path(me.id) %>
        </li>
        <li>
        <%= @upcoming_events.title %>
        </li>
        <li>
        <%= @upcoming_events.description %>
        </li>
        <li>
        <%= @upcoming_events.venue %>
        </li>
        <li>
        <%= @upcoming_events.date %>
        </li>
      <% end %>
    </ul>
  </div>
<% end %>